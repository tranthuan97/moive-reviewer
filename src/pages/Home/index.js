import React from 'react';
import PropTypes from 'prop-types';
import Header from './components/Header';

const Home = props => {
  return (
    <div>
      <Header />
    </div>
  );
};

Home.propTypes = {
  
};

export default Home;